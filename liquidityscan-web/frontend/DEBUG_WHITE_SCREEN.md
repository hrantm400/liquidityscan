# 🔍 Отладка белого экрана

## ✅ Что было исправлено:

1. **CSS ошибка** - `@import` теперь перед `@tailwind` директив
2. **ErrorBoundary** - добавлен для отлова ошибок
3. **Упрощены классы** - убраны дублирующиеся dark: классы
4. **Проверка root элемента** - добавлена проверка в main.tsx

## 🔍 Как проверить, что происходит:

### 1. Откройте консоль браузера (F12)
   - Вкладка Console
   - Ищите красные ошибки

### 2. Проверьте Network вкладку
   - Убедитесь, что все файлы загружаются (статус 200)
   - Проверьте, что нет 404 ошибок

### 3. Проверьте React DevTools
   - Установите расширение React DevTools
   - Посмотрите, что рендерится

### 4. Проверьте, что backend работает
   - Откройте http://localhost:3000/api/health
   - Должен вернуться `{"status":"ok"}`

## 🚀 Быстрая проверка:

1. Откройте http://localhost:5173
2. Нажмите F12
3. Посмотрите вкладку Console
4. Если есть ошибки - скопируйте их

## 📝 Возможные причины белого экрана:

1. **Ошибка JavaScript** - проверьте консоль
2. **Проблема с API** - проверьте, что backend запущен
3. **Проблема с роутингом** - проверьте, что все страницы существуют
4. **Проблема с CSS** - проверьте, что Tailwind правильно настроен

## 🔧 Если все еще белый экран:

1. Очистите кеш браузера (Ctrl+Shift+Delete)
2. Перезапустите frontend: `npm run dev`
3. Проверьте, что все зависимости установлены: `npm install`
4. Проверьте версию Node.js: `node --version` (должна быть 18+)
