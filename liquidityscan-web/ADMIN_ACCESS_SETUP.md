# Настройка доступа к админ панели

## Как работает доступ к админ панели

Доступ к админ панели контролируется двумя проверками:
1. **Поле `isAdmin` в базе данных** - должно быть `true`
2. **Email в списке `ADMIN_EMAILS`** - email пользователя должен быть в списке

**Обе проверки должны пройти успешно!**

## Настройка в .env файле

Откройте файл `backend/.env` и добавьте переменную `ADMIN_EMAILS`:

```env
ADMIN_EMAILS=admin@example.com,superadmin@example.com,another@example.com
```

### Формат

- **Через запятую** - несколько email адресов разделяются запятой
- **Без пробелов** (или с пробелами, они будут автоматически удалены)
- **Регистр не важен** - `Admin@Example.com` и `admin@example.com` - одно и то же

### Примеры

**Один админ:**
```env
ADMIN_EMAILS=admin@liquidityscan.com
```

**Несколько админов:**
```env
ADMIN_EMAILS=admin@liquidityscan.com,superadmin@liquidityscan.com,manager@liquidityscan.com
```

**С пробелами (будут удалены автоматически):**
```env
ADMIN_EMAILS=admin@example.com, superadmin@example.com, manager@example.com
```

## Автоматическое назначение прав админа

При регистрации или входе через Google OAuth:

- Если email пользователя **есть в списке `ADMIN_EMAILS`**, то `isAdmin` автоматически устанавливается в `true`
- Если email **нет в списке**, то `isAdmin` остается `false`

## Как добавить админа

### Способ 1: Через регистрацию (автоматически)

1. Добавьте email в `ADMIN_EMAILS` в `.env`:
   ```env
   ADMIN_EMAILS=your-email@example.com
   ```

2. Перезапустите бэкенд:
   ```bash
   npm run start:dev
   ```

3. Зарегистрируйтесь с этим email через:
   - Обычную регистрацию (`/api/auth/register`)
   - Google OAuth

4. После регистрации пользователь автоматически получит права админа

### Способ 2: Вручную через базу данных

Если пользователь уже существует:

1. Откройте Prisma Studio:
   ```bash
   cd backend
   npm run prisma:studio
   ```

2. Найдите пользователя по email
3. Измените `isAdmin` на `true`
4. Убедитесь, что email добавлен в `ADMIN_EMAILS` в `.env`

### Способ 3: Через SQL

```sql
UPDATE users 
SET "isAdmin" = true 
WHERE email = 'admin@example.com';
```

**Важно:** Email также должен быть в `ADMIN_EMAILS` в `.env`!

## Проверка доступа

После настройки проверьте:

1. **Войдите в систему** с email из `ADMIN_EMAILS`
2. **В боковом меню** появится раздел **"ADMIN"** с кнопкой **"Admin Panel"**
3. **Нажмите на "Admin Panel"** или перейдите на `/admin`
4. **Должна открыться админ панель**

### Где найти кнопку админ панели?

Кнопка **"Admin Panel"** находится в боковом меню (Sidebar) слева:
- Раздел называется **"ADMIN"**
- Кнопка с иконкой и текстом **"Admin Panel"**
- Видна **только для пользователей с правами админа**
- Выделена зеленым цветом (primary) для лучшей видимости
- Расположена между разделом "ACADEMY" и "Other Pages"

Если доступ запрещен:
- Проверьте, что email точно совпадает с email в `ADMIN_EMAILS` (регистр не важен)
- Проверьте, что `isAdmin = true` в базе данных
- Перезапустите бэкенд после изменения `.env`

## Безопасность

⚠️ **Важные замечания:**

1. **Никогда не коммитьте `.env` файл** в Git
2. **В продакшене** используйте переменные окружения сервера, а не `.env`
3. **Регулярно проверяйте** список админов
4. **Используйте сильные пароли** для админ аккаунтов
5. **Ограничьте количество** админов (только необходимые)

## Пример полного .env файла

```env
# Database (local PostgreSQL - port 5432)
DATABASE_URL="postgresql://postgres:password@localhost:5432/liquidityscan_db?schema=public"

# JWT
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your-super-secret-refresh-key
JWT_REFRESH_EXPIRES_IN=30d

# Admin Access - список email адресов с доступом к админ панели
ADMIN_EMAILS=admin@liquidityscan.com,superadmin@liquidityscan.com

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback

# Server
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
FRONTEND_URL=http://localhost:5173
```

## Устранение проблем

### Проблема: "Admin access required"

**Решение:**
1. Проверьте, что email точно совпадает с email в `ADMIN_EMAILS`
2. Проверьте, что `isAdmin = true` в базе данных
3. Перезапустите бэкенд после изменения `.env`

### Проблема: Email в списке, но доступ запрещен

**Решение:**
1. Убедитесь, что в `.env` нет лишних пробелов или символов
2. Проверьте формат: `email1@example.com,email2@example.com` (через запятую)
3. Перезапустите бэкенд

### Проблема: После регистрации нет прав админа

**Решение:**
1. Убедитесь, что `ADMIN_EMAILS` настроен **до** регистрации
2. Перезапустите бэкенд
3. Зарегистрируйтесь заново или обновите `isAdmin` вручную

## Быстрая проверка

```bash
# 1. Проверьте .env
cat backend/.env | grep ADMIN_EMAILS

# 2. Перезапустите бэкенд
cd backend
npm run start:dev

# 3. Зарегистрируйтесь с email из списка
# 4. Попробуйте зайти на /admin
```

Готово! 🎉
