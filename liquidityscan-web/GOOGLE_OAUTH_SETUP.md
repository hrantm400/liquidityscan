# Настройка Google OAuth для LiquidityScan

Пошаговая инструкция по настройке Google OAuth для входа через Google аккаунт.

## Шаг 1: Создание проекта в Google Cloud Console

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Войдите в свой Google аккаунт
3. Нажмите на выпадающий список проектов в верхней панели
4. Нажмите **"New Project"** (Новый проект)
5. Введите название проекта: `LiquidityScan` (или любое другое)
6. Нажмите **"Create"** (Создать)
7. Дождитесь создания проекта (обычно несколько секунд)

## Шаг 2: Включение Google+ API

1. В меню слева выберите **"APIs & Services"** → **"Library"** (Библиотека)
2. В поиске введите: `Google+ API`
3. Нажмите на **"Google+ API"** в результатах
4. Нажмите кнопку **"Enable"** (Включить)
5. Дождитесь активации API

**Альтернатива:** Можно использовать **"Google Identity"** вместо Google+ API (более современный вариант)

## Шаг 3: Создание OAuth 2.0 Credentials

1. В меню слева выберите **"APIs & Services"** → **"Credentials"** (Учетные данные)
2. Нажмите кнопку **"+ CREATE CREDENTIALS"** (Создать учетные данные)
3. Выберите **"OAuth client ID"** (OAuth клиент ID)

### Настройка OAuth consent screen (если запросит)

Если это первый раз, Google попросит настроить OAuth consent screen:

1. Выберите **"External"** (Внешний) и нажмите **"Create"**
2. Заполните обязательные поля:
   - **App name** (Название приложения): `LiquidityScan`
   - **User support email** (Email поддержки): ваш email
   - **Developer contact information** (Контакт разработчика): ваш email
3. Нажмите **"Save and Continue"** (Сохранить и продолжить)
4. На шаге **"Scopes"** нажмите **"Save and Continue"** (можно пропустить)
5. На шаге **"Test users"** нажмите **"Save and Continue"** (можно пропустить)
6. Нажмите **"Back to Dashboard"** (Вернуться к панели)

### Создание OAuth Client ID

1. В разделе **"Application type"** (Тип приложения) выберите **"Web application"** (Веб-приложение)
2. Введите **Name** (Имя): `LiquidityScan Web Client`
3. В разделе **"Authorized JavaScript origins"** (Разрешенные источники JavaScript) нажмите **"+ ADD URI"** и добавьте:
   ```
   http://localhost:5173
   ```
   (Для продакшена добавьте ваш домен, например: `https://yourdomain.com`)

4. В разделе **"Authorized redirect URIs"** (Разрешенные URI перенаправления) нажмите **"+ ADD URI"** и добавьте:
   ```
   http://localhost:3000/api/auth/google/callback
   ```
   (Для продакшена добавьте: `https://yourdomain.com/api/auth/google/callback`)

5. Нажмите **"Create"** (Создать)

## Шаг 4: Получение Client ID и Client Secret

После создания OAuth Client вы увидите модальное окно с:
- **Your Client ID** (Ваш Client ID) - скопируйте это значение
- **Your Client Secret** (Ваш Client Secret) - скопируйте это значение

**Важно:** Client Secret показывается только один раз! Обязательно скопируйте его.

Если вы случайно закрыли окно:
1. Перейдите в **"APIs & Services"** → **"Credentials"**
2. Найдите ваш OAuth 2.0 Client ID в списке
3. Нажмите на него
4. Client ID будет виден, но Client Secret нужно будет сгенерировать заново (кнопка "Reset secret")

## Шаг 5: Настройка в проекте

1. Откройте файл `backend/.env` (если его нет, скопируйте из `.env.example`)

2. Добавьте следующие строки:
   ```env
   GOOGLE_CLIENT_ID=ваш_client_id_здесь
   GOOGLE_CLIENT_SECRET=ваш_client_secret_здесь
   GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback
   ```

3. Замените `ваш_client_id_здесь` и `ваш_client_secret_здесь` на реальные значения из Google Cloud Console

4. Сохраните файл

## Шаг 6: Перезапуск бэкенда

1. Остановите бэкенд (если запущен): `Ctrl+C` в терминале
2. Запустите снова:
   ```bash
   cd backend
   npm run start:dev
   ```

3. Вы должны увидеть, что приложение запустилось без предупреждений о Google OAuth

## Шаг 7: Проверка работы

1. Откройте фронтенд: `http://localhost:5173`
2. Перейдите на страницу входа: `http://localhost:5173/login`
3. Нажмите кнопку **"Sign in with Google"** (Войти через Google)
4. Должно произойти перенаправление на Google для авторизации
5. После авторизации вы будете перенаправлены обратно в приложение

## Для продакшена

Когда будете готовы к продакшену:

1. В Google Cloud Console:
   - Добавьте ваш домен в **Authorized JavaScript origins**
   - Добавьте ваш callback URL в **Authorized redirect URIs**
   - В **OAuth consent screen** измените статус с "Testing" на "In production" (после проверки Google)

2. В `backend/.env` обновите:
   ```env
   GOOGLE_CALLBACK_URL=https://yourdomain.com/api/auth/google/callback
   FRONTEND_URL=https://yourdomain.com
   ```

## Возможные проблемы

### Ошибка: "redirect_uri_mismatch"
**Решение:** Убедитесь, что URL в `.env` точно совпадает с URL в Google Cloud Console (включая `http://` или `https://`, порт, путь)

### Ошибка: "access_denied"
**Решение:** Проверьте, что OAuth consent screen настроен правильно и приложение не в режиме тестирования (или добавьте тестовых пользователей)

### Ошибка: "invalid_client"
**Решение:** Проверьте, что GOOGLE_CLIENT_ID и GOOGLE_CLIENT_SECRET правильно скопированы в `.env` файл

### Google OAuth не работает после настройки
**Решение:**
1. Убедитесь, что бэкенд перезапущен после изменения `.env`
2. Проверьте логи бэкенда на наличие ошибок
3. Убедитесь, что порты 3000 (бэкенд) и 5173 (фронтенд) доступны

## Безопасность

⚠️ **Важно:**
- Никогда не коммитьте `.env` файл в Git
- Client Secret должен храниться в секретах
- В продакшене используйте переменные окружения сервера, а не `.env` файл
- Регулярно обновляйте Client Secret

## Дополнительные ресурсы

- [Google OAuth 2.0 Documentation](https://developers.google.com/identity/protocols/oauth2)
- [Google Cloud Console](https://console.cloud.google.com/)
- [NestJS Passport Google Strategy](https://docs.nestjs.com/security/authentication)

## Быстрая проверка

После настройки проверьте, что все работает:

```bash
# В backend/.env должны быть:
GOOGLE_CLIENT_ID=ваш_id
GOOGLE_CLIENT_SECRET=ваш_secret
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback

# Перезапустите бэкенд
npm run start:dev

# Должно запуститься без ошибок
```

Готово! 🎉
