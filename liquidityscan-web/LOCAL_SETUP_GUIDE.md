# 📘 Полное руководство по локальной настройке Liquidity Scan Web Application

Этот документ содержит максимально подробные инструкции для локальной настройки и запуска приложения на вашем компьютере.

---

## 📋 Содержание

1. [Проверка системных требований](#1-проверка-системных-требований)
2. [Установка необходимого ПО](#2-установка-необходимого-по)
3. [Клонирование и подготовка проекта](#3-клонирование-и-подготовка-проекта)
4. [Настройка PostgreSQL](#4-настройка-postgresql)
5. [Настройка базы данных](#5-настройка-базы-данных)
6. [Настройка Backend](#6-настройка-backend)
7. [Настройка Frontend](#7-настройка-frontend)
8. [Запуск приложения](#8-запуск-приложения)
9. [Проверка работоспособности](#9-проверка-работоспособности)
10. [Решение проблем](#10-решение-проблем)

---

## 1. Проверка системных требований

### Минимальные требования:

- **Операционная система:** Windows 10/11, macOS, или Linux
- **RAM:** Минимум 4GB (рекомендуется 8GB)
- **Свободное место:** Минимум 5GB
- **Процессор:** Любой современный процессор

### Необходимое ПО:

Перед началом убедитесь, что у вас установлено:

1. **Node.js** версии 18 или выше
2. **npm** (обычно устанавливается вместе с Node.js)
3. **PostgreSQL** (локальная установка, порт 5432)
4. **Git** (опционально, если клонируете из репозитория)

---

## 2. Установка необходимого ПО

### Шаг 2.1: Установка Node.js

#### Для Windows:

1. Перейдите на сайт: https://nodejs.org/
2. Скачайте LTS версию (рекомендуется 18.x или выше)
3. Запустите установщик `.msi`
4. Следуйте инструкциям установщика
5. **Важно:** Убедитесь, что опция "Add to PATH" включена

#### Проверка установки:

Откройте PowerShell или Command Prompt и выполните:

```powershell
node --version
npm --version
```

Должны отобразиться версии, например:
```
v18.17.0
9.6.7
```

Если команды не работают, перезапустите терминал или перезагрузите компьютер.

---

### Шаг 2.2: Установка PostgreSQL

#### Для Windows:

1. Перейдите на сайт: https://www.postgresql.org/download/windows/
2. Скачайте установщик PostgreSQL (рекомендуется версия 15 или выше)
3. Запустите установщик и следуйте инструкциям
4. Запомните пароль суперпользователя `postgres` (или задайте свой)
5. Порт по умолчанию: **5432** (оставьте как есть)
6. Убедитесь, что служба PostgreSQL запускается автоматически

#### Проверка установки:

Откройте PowerShell и выполните (если `psql` в PATH):

```powershell
psql --version
```

Либо проверьте в «Службы» Windows, что служба **postgresql-x64-15** (или ваша версия) запущена.

---

## 3. Клонирование и подготовка проекта

### Шаг 3.1: Переход в директорию проекта

Откройте PowerShell или Command Prompt и перейдите в папку с проектом:

```powershell
cd "C:\Users\hrant\Desktop\liquidityscan -MEGA\liquidityscan-web"
```

Проверьте, что вы в правильной директории:

```powershell
dir
```

Должны увидеть папки: `backend`, `frontend`

---

## 4. Настройка PostgreSQL

### Шаг 4.1: Запуск службы PostgreSQL

Убедитесь, что служба PostgreSQL запущена на порту **5432** (в «Службы» Windows или через pg_ctl).

### Шаг 4.2: Создание базы данных и пользователя (опционально)

Можно использовать учётную запись `postgres` или создать отдельного пользователя и БД:

1. Откройте pgAdmin или выполните в терминале (если `psql` в PATH):
   ```powershell
   psql -U postgres
   ```
2. В консоли PostgreSQL:
   ```sql
   CREATE DATABASE liquidityscan_db;
   -- Опционально: CREATE USER liquidityscan WITH PASSWORD 'your_password';
   -- GRANT ALL PRIVILEGES ON DATABASE liquidityscan_db TO liquidityscan;
   ```

Если используете только пользователя `postgres`, просто создайте базу:
```sql
CREATE DATABASE liquidityscan_db;
```

### Шаг 4.3: Проверка подключения

Убедитесь, что к БД можно подключиться с теми учётными данными, которые будете указывать в `DATABASE_URL` (хост localhost, порт 5432).

---

## 5. Настройка базы данных

### Шаг 5.1: Переход в директорию backend

```powershell
cd backend
```

### Шаг 5.2: Установка зависимостей backend

```powershell
npm install
```

**Что происходит:**
- Устанавливаются все npm пакеты из `package.json`
- Это может занять 2-5 минут

**Ожидаемый результат:**
```
added 880 packages, and audited 881 packages in 45s
```

### Шаг 5.3: Создание файла .env

Создайте файл `.env` в папке `backend`:

**Способ 1: Через PowerShell**

```powershell
New-Item -Path .env -ItemType File
```

**Способ 2: Вручную**
- Откройте папку `backend` в проводнике
- Создайте новый текстовый файл
- Переименуйте его в `.env` (включая точку в начале!)

### Шаг 5.4: Заполнение .env файла

Откройте файл `.env` в любом текстовом редакторе (Notepad++, VS Code, или обычный Блокнот). Скопируйте содержимое из `backend/.env.example` и подставьте свои значения:

```env
# Database (локальный PostgreSQL - порт 5432)
DATABASE_URL="postgresql://postgres:password@localhost:5432/liquidityscan_db?schema=public"

# JWT - ВАЖНО: Измените эти значения на свои!
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this-in-production
JWT_REFRESH_EXPIRES_IN=30d

# Admin Access - список email адресов с доступом к админ панели (через запятую)
ADMIN_EMAILS=admin@example.com,superadmin@example.com

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback

# Server
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
FRONTEND_URL=http://localhost:5173
```

**Важно:**
- Замените `postgres` и `password` в `DATABASE_URL` на ваши учётные данные PostgreSQL.
- Измените `JWT_SECRET` и `JWT_REFRESH_SECRET` на случайные строки (минимум 32 символа). Для генерации можно использовать: https://randomkeygen.com/

### Шаг 5.5: Создание миграций базы данных

**ВАЖНО:** Если у вас Prisma 7, используйте альтернативный метод ниже.

#### Метод 1: Использование db push (рекомендуется для Prisma 7)

Выполните:

```powershell
npx prisma db push
```

**Что происходит:**
- Prisma создает структуру базы данных напрямую
- Создает все таблицы: users, signals, candles, strategies, subscriptions, signal_alerts
- Не создает файлы миграций (но это нормально для разработки)

**Ожидаемый результат:**
```
Prisma schema loaded from prisma\schema.prisma
Datasource "db": PostgreSQL database "liquidityscan_db", schema "public" at "localhost:5432"

✔ Generated Prisma Client

The database is now in sync with your Prisma schema.
```

#### Метод 2: Использование миграций

```powershell
npx prisma migrate dev --name init
```

**Если видите ошибку подключения к базе данных:**
1. Убедитесь, что служба PostgreSQL запущена на порту 5432
2. Проверьте `DATABASE_URL` в `.env` (хост, порт 5432, пользователь, пароль, имя БД)
3. Используйте Метод 1 (db push) вместо миграций

### Шаг 5.6: Генерация Prisma Client

Выполните:

```powershell
npx prisma generate
```

**Ожидаемый результат:**
```
✔ Generated Prisma Client to .\node_modules\.prisma\client
```

**Важно:** Если вы использовали `db push`, Prisma Client уже сгенерирован автоматически. Но можно запустить команду еще раз для уверенности.

---

## 6. Настройка Backend

### Шаг 6.1: Проверка структуры backend

Убедитесь, что вы все еще в папке `backend`:

```powershell
pwd
```

Должно быть: `C:\Users\hrant\Desktop\liquidityscan -MEGA\liquidityscan-web\backend`

### Шаг 6.2: Проверка установленных пакетов

```powershell
npm list --depth=0
```

Должны увидеть основные пакеты:
- @nestjs/core
- @prisma/client
- socket.io
- и другие

### Шаг 6.3: Тестовый запуск backend

Выполните:

```powershell
npm run start:dev
```

**Что происходит:**
- Компилируется TypeScript код
- Запускается NestJS сервер
- Подключается к базе данных
- Запускается на порту 3000

**Ожидаемый результат:**
```
[Nest] 12345  - 01/07/2024, 2:00:00 PM     LOG [NestFactory] Starting Nest application...
[Nest] 12345  - 01/07/2024, 2:00:00 PM     LOG [InstanceLoader] AppModule dependencies initialized
[Nest] 12345  - 01/07/2024, 2:00:00 PM     LOG [InstanceLoader] PrismaModule dependencies initialized
...
Application is running on: http://localhost:3000
```

**Если видите ошибки:**
- Проверьте, что служба PostgreSQL запущена на порту 5432
- Проверьте файл `.env`: правильный ли `DATABASE_URL` (хост, порт 5432, пользователь, пароль)
- Выполните проверку подключения: `npm run db:check`

**Остановка сервера:**
Нажмите `Ctrl + C` в терминале

---

## 7. Настройка Frontend

### Шаг 7.1: Переход в директорию frontend

Откройте **новый** терминал PowerShell (старый оставьте для backend) и выполните:

```powershell
cd "C:\Users\hrant\Desktop\liquidityscan -MEGA\liquidityscan-web\frontend"
```

### Шаг 7.2: Установка зависимостей frontend

```powershell
npm install
```

**Что происходит:**
- Устанавливаются все npm пакеты для React приложения
- Это может занять 2-5 минут

**Ожидаемый результат:**
```
added 218 packages, and audited 219 packages in 30s
```

### Шаг 7.3: Создание файла .env для frontend

Создайте файл `.env` в папке `frontend`:

```powershell
New-Item -Path .env -ItemType File
```

### Шаг 7.4: Заполнение .env файла frontend

Откройте файл `.env` и вставьте:

```env
VITE_API_URL=http://localhost:3000
VITE_WS_URL=http://localhost:3000
```

**Важно:** Не добавляйте пробелы вокруг `=`!

### Шаг 7.5: Тестовый запуск frontend

Выполните:

```powershell
npm run dev
```

**Что происходит:**
- Запускается Vite dev сервер
- Компилируется React приложение
- Открывается на порту 5173

**Ожидаемый результат:**
```
  VITE v5.0.0  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

**Остановка сервера:**
Нажмите `Ctrl + C` в терминале

---

## 8. Запуск приложения

### Шаг 8.1: Запуск всех сервисов

Убедитесь, что служба PostgreSQL запущена на порту 5432. Затем запустите backend и frontend:

#### Терминал 1: Backend

```powershell
cd "C:\Users\hrant\Desktop\liquidityscan -MEGA\liquidityscan-web\backend"
npm run start:dev
```

Дождитесь сообщения:
```
Application is running on: http://localhost:3000
```

#### Терминал 2: Frontend

```powershell
cd "C:\Users\hrant\Desktop\liquidityscan -MEGA\liquidityscan-web\frontend"
npm run dev
```

Дождитесь сообщения:
```
➜  Local:   http://localhost:5173/
```

### Шаг 8.2: Открытие приложения в браузере

1. Откройте браузер (Chrome, Firefox, Edge)
2. Перейдите по адресу: **http://localhost:5173**
3. Должна открыться главная страница приложения

---

## 9. Проверка работоспособности

### Шаг 9.1: Проверка Backend API

Откройте браузер и перейдите:

```
http://localhost:3000/api/health
```

**Ожидаемый результат:**
```json
{
  "status": "ok",
  "timestamp": "2024-01-07T14:00:00.000Z"
}
```

Если видите ошибку:
- Убедитесь, что backend запущен
- Проверьте порт 3000 не занят другим приложением

### Шаг 9.2: Проверка Frontend

1. Откройте: http://localhost:5173
2. Должна загрузиться страница с навигацией
3. Попробуйте переключить тему (иконка в header)
4. Попробуйте открыть разные страницы:
   - Super Engulfing
   - Bias Shifts
   - RSI Divergence

### Шаг 9.3: Проверка базы данных

В терминале backend выполните:

```powershell
npx prisma studio
```

Откроется веб-интерфейс Prisma Studio на http://localhost:5555

Здесь вы можете:
- Просматривать таблицы
- Добавлять/редактировать данные
- Проверять структуру БД

---

## 10. Решение проблем

### Проблема 1: Ошибка подключения к базе данных

**Симптомы:**
```
Error: P1001: Can't reach database server
```

**Решения:**
1. Убедитесь, что служба PostgreSQL запущена на порту 5432 (проверьте в «Службы» Windows или через pg_ctl).
2. Проверьте `DATABASE_URL` в `backend/.env`: хост localhost, порт 5432, пользователь, пароль, имя БД.
3. Убедитесь, что база данных существует (например, `liquidityscan_db`).
4. Выполните проверку подключения: `cd backend && npm run db:check`

---

### Проблема 2: Порт уже занят

**Симптомы:**
```
Error: listen EADDRINUSE: address already in use :::3000
```

**Решения:**
1. Найдите процесс, использующий порт:
   ```powershell
   netstat -ano | findstr :3000
   ```
2. Завершите процесс или измените PORT в `.env`

---

### Проблема 3: Ошибки при npm install

**Симптомы:**
```
npm ERR! code ERESOLVE
npm ERR! ERESOLVE unable to resolve dependency tree
```

**Решения:**
1. Очистите кэш npm:
   ```powershell
   npm cache clean --force
   ```
2. Удалите папку `node_modules` и файл `package-lock.json`
3. Выполните установку заново:
   ```powershell
   npm install
   ```

---

### Проблема 4: Prisma миграции не применяются

**Симптомы:**
```
Error: Migration engine failed to connect to database
```

**Решения:**
1. Убедитесь, что база данных запущена
2. Проверьте DATABASE_URL в `.env`
3. Попробуйте сбросить базу данных:
   ```powershell
   npx prisma migrate reset
   ```
   **Внимание:** Это удалит все данные!

---

### Проблема 5: Frontend не подключается к Backend

**Симптомы:**
- В браузере видна ошибка в консоли
- "Failed to fetch" или "Network error"

**Решения:**
1. Убедитесь, что backend запущен на порту 3000
2. Проверьте файл `.env` frontend - правильный ли VITE_API_URL
3. Проверьте CORS настройки в backend
4. Откройте консоль браузера (F12) и проверьте ошибки

---

## 📝 Полезные команды

### Backend команды:

```powershell
# Запуск в режиме разработки
npm run start:dev

# Сборка для production
npm run build

# Запуск production версии
npm run start:prod

# Просмотр Prisma Studio
npx prisma studio

# Создание новой миграции
npx prisma migrate dev --name migration_name

# Применение миграций
npx prisma migrate deploy
```

### Frontend команды:

```powershell
# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Просмотр production сборки
npm run preview
```

---

## ✅ Чек-лист успешной установки

Отметьте выполненные пункты:

- [ ] Node.js установлен и работает
- [ ] PostgreSQL установлен и служба запущена (порт 5432)
- [ ] Backend зависимости установлены
- [ ] Backend .env файл создан и заполнен
- [ ] База данных миграции применены
- [ ] Backend запускается без ошибок
- [ ] Frontend зависимости установлены
- [ ] Frontend .env файл создан и заполнен
- [ ] Frontend запускается без ошибок
- [ ] Приложение открывается в браузере
- [ ] API health check работает
- [ ] Страницы приложения загружаются

---

## 🎉 Готово!

Если все пункты чек-листа отмечены, приложение успешно настроено и готово к использованию!

**Следующие шаги:**
1. Изучите интерфейс приложения
2. Попробуйте все страницы
3. Настройте API ключи для работы с биржами (опционально)
4. Начните разработку новых функций

---

## 📞 Нужна помощь?

Если возникли проблемы, которые не описаны в этом руководстве:

1. Проверьте, что PostgreSQL запущен на порту 5432
2. Проверьте логи backend в терминале
3. Проверьте консоль браузера (F12)
4. Убедитесь, что все шаги выполнены правильно

---

**Удачной разработки! 🚀**
