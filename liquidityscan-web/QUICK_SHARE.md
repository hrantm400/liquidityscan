# 🚀 Быстрый способ поделиться проектом

## Самый простой способ с Cloudflare Tunnel (БЕСПЛАТНО, БЫСТРО, БЕЗЛИМИТНО!):

### 1. Установите Cloudflare Tunnel (cloudflared):
```bash
# Windows (через Chocolatey)
choco install cloudflared

# Windows (вручную)
# Скачайте с https://github.com/cloudflare/cloudflared/releases
# Распакуйте cloudflared.exe в папку в PATH

# Или через npm
npm install -g cloudflared
```

### 2. Запустите проект:

**Терминал 1 - Docker:**
```bash
cd liquidityscan-web
docker-compose up -d
```

**Терминал 2 - Backend:**
```bash
cd liquidityscan-web/backend
npm run start:dev
```

**Терминал 3 - Frontend:**
```bash
cd liquidityscan-web/frontend
npm run dev
```

**Терминал 4 - Cloudflare Tunnel для Frontend:**
```bash
cloudflared tunnel --url http://localhost:5173
```

**Терминал 5 - Cloudflare Tunnel для Backend (опционально, если нужен прямой доступ к API):**
```bash
cloudflared tunnel --url http://localhost:3000
```

### 3. Обновите CORS:

Скопируйте HTTPS URL из Cloudflare Tunnel (например: `https://xxxx-xxxx.trycloudflare.com`)

В файле `liquidityscan-web/backend/.env` добавьте:
```env
CORS_ORIGIN=https://xxxx-xxxx.trycloudflare.com
```

Перезапустите Backend.

### 4. Дай товарищу ссылку:

Скопируйте HTTPS URL из Cloudflare Tunnel и дай товарищу.

**Полная ссылка для доступа:**
```
https://xxxx-xxxx.trycloudflare.com/app/dashboard
```

## ✅ Преимущества Cloudflare Tunnel:

- ✅ **БЕСПЛАТНО** - полностью бесплатный
- ✅ **БЫСТРО** - использует сеть Cloudflare (быстрее чем ngrok)
- ✅ **БЕЗЛИМИТНО** - нет ограничений на трафик или время
- ✅ **БЕЗ РЕГИСТРАЦИИ** - работает сразу, не нужен аккаунт
- ✅ **СТАБИЛЬНО** - более надежный чем ngrok
- ✅ **HTTPS** - автоматически с SSL сертификатом

## ⚠️ Важно:

- Ссылка меняется при каждом перезапуске (для постоянной ссылки нужна настройка через Cloudflare Zero Trust)
- Это только для демонстрации, не для продакшена!

## Альтернатива (если товарищ в той же WiFi):

Если ваш товарищ в той же локальной сети, можно использовать локальный IP:

**Ваш IP:** `10.187.18.53` или `172.28.144.1`

1. Обновите `frontend/vite.config.ts`:
```typescript
server: {
  host: '0.0.0.0', // Добавьте эту строку
  // ... остальное
}
```

2. Обновите `backend/.env`:
```env
CORS_ORIGIN=http://10.187.18.53:5173
```

3. Дай товарищу ссылку:
```
http://10.187.18.53:5173/app/dashboard
```
