# Environment variables and secrets (do not commit)
.env
.env.local
.env.*.local

# Dependencies and build output (install/build on server)
node_modules/
dist/
**/dist/

# Java / other builds (not needed for LiquidityScan)
*.jar
tradingBot.jar
