# Чеклист безопасности перед развертыванием

Этот чеклист поможет вам безопасно развернуть домашнюю страницу, не затронув существующие веб-сайты и боты.

## ✅ Шаг 1: Проверка окружения сервера

- [ ] Выполните скрипт проверки окружения:
  ```bash
  # Windows:
  .\deploy\check-server-env.ps1 -ServerUser "root" -ServerIP "YOUR_IP"
  
  # Linux/macOS:
  ./deploy/check-server-env.sh root YOUR_IP
  ```

- [ ] Запишите результаты проверки:
  - [ ] Какой веб-сервер установлен? (Nginx / Apache / оба)
  - [ ] Какие домены уже настроены?
  - [ ] Какие директории используются?
  - [ ] Какие порты заняты?

## ✅ Шаг 2: Проверка нового домена

- [ ] Убедитесь, что новый домен НЕ конфликтует с существующими:
  ```bash
  # На сервере проверьте существующие домены:
  # Для Nginx:
  grep -r "yourdomain.com" /etc/nginx/sites-enabled/
  
  # Для Apache:
  grep -r "yourdomain.com" /etc/apache2/sites-enabled/
  ```

- [ ] Если домен уже используется, выберите другой домен или поддомен

## ✅ Шаг 3: Подготовка директории

- [ ] Проверьте, что директория `/var/www/yourdomain.com` не существует:
  ```bash
  ls -la /var/www/yourdomain.com
  ```
  
- [ ] Если директория существует и используется другим сайтом, выберите другое имя

- [ ] Создайте новую директорию только для вашего домена:
  ```bash
  sudo mkdir -p /var/www/yourdomain.com
  ```

## ✅ Шаг 4: Проверка конфигурационного файла

- [ ] Убедитесь, что файл конфигурации еще не существует:
  ```bash
  # Для Nginx:
  ls -la /etc/nginx/sites-available/yourdomain.com
  ls -la /etc/nginx/sites-enabled/yourdomain.com
  
  # Для Apache:
  ls -la /etc/apache2/sites-available/yourdomain.com.conf
  ls -la /etc/apache2/sites-enabled/yourdomain.com.conf
  ```

- [ ] Если файлы существуют, проверьте их содержимое или используйте другое имя

## ✅ Шаг 5: Создание конфигурации

- [ ] Скопируйте шаблон конфигурации:
  - Для Nginx: `deploy/nginx-homepage.conf`
  - Для Apache: `deploy/apache-homepage.conf`

- [ ] Замените `yourdomain.com` на ваш фактический домен во ВСЕХ местах

- [ ] Убедитесь, что `server_name` (Nginx) или `ServerName` (Apache) не конфликтует с существующими

- [ ] Убедитесь, что `root` (Nginx) или `DocumentRoot` (Apache) указывает на `/var/www/yourdomain.com`

## ✅ Шаг 6: Проверка конфигурации перед применением

- [ ] **КРИТИЧЕСКИ ВАЖНО:** Проверьте конфигурацию перед применением:
  ```bash
  # Для Nginx:
  sudo nginx -t
  
  # Для Apache:
  sudo apache2ctl configtest
  # или
  sudo httpd -t
  ```

- [ ] Если есть ошибки, исправьте их ПЕРЕД активацией конфигурации

## ✅ Шаг 7: Активация конфигурации

- [ ] Активируйте конфигурацию:
  ```bash
  # Для Nginx:
  sudo ln -s /etc/nginx/sites-available/yourdomain.com /etc/nginx/sites-enabled/
  
  # Для Apache:
  sudo a2ensite yourdomain.com.conf
  ```

- [ ] Еще раз проверьте конфигурацию после активации:
  ```bash
  # Для Nginx:
  sudo nginx -t
  
  # Для Apache:
  sudo apache2ctl configtest
  ```

## ✅ Шаг 8: Перезагрузка веб-сервера

- [ ] Перезагрузите веб-сервер (НЕ останавливайте, только перезагрузите):
  ```bash
  # Для Nginx:
  sudo systemctl reload nginx
  
  # Для Apache:
  sudo systemctl reload apache2
  # или
  sudo systemctl reload httpd
  ```

- [ ] Проверьте, что веб-сервер работает:
  ```bash
  # Для Nginx:
  sudo systemctl status nginx
  
  # Для Apache:
  sudo systemctl status apache2
  # или
  sudo systemctl status httpd
  ```

## ✅ Шаг 9: Проверка существующих сервисов

- [ ] Убедитесь, что существующие сайты все еще работают:
  - [ ] Откройте существующие домены в браузере
  - [ ] Проверьте, что они загружаются нормально

- [ ] Убедитесь, что боты все еще работают:
  - [ ] Проверьте логи ботов
  - [ ] Проверьте статус процессов ботов

## ✅ Шаг 10: Тестирование нового домена

- [ ] Проверьте новый домен (после настройки DNS):
  ```bash
  curl -I http://yourdomain.com
  ```

- [ ] Откройте домен в браузере и убедитесь, что он работает

## ⚠️ Что НЕ делать:

- ❌ **НЕ изменяйте** существующие конфигурационные файлы
- ❌ **НЕ используйте** существующие директории веб-серверов
- ❌ **НЕ останавливайте** существующие сервисы
- ❌ **НЕ удаляйте** существующие конфигурации
- ❌ **НЕ используйте** существующие домены
- ❌ **НЕ применяйте** конфигурацию без проверки (`nginx -t` / `apache2ctl configtest`)

## 🔧 Если что-то пошло не так:

1. **Откатите изменения:**
   ```bash
   # Для Nginx:
   sudo rm /etc/nginx/sites-enabled/yourdomain.com
   sudo systemctl reload nginx
   
   # Для Apache:
   sudo a2dissite yourdomain.com.conf
   sudo systemctl reload apache2
   ```

2. **Проверьте логи:**
   ```bash
   # Для Nginx:
   sudo tail -f /var/log/nginx/error.log
   
   # Для Apache:
   sudo tail -f /var/log/apache2/error.log
   ```

3. **Восстановите работу существующих сервисов:**
   - Проверьте, что существующие конфигурации не изменены
   - Перезагрузите веб-сервер
   - Проверьте статус всех сервисов

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи веб-сервера
2. Проверьте конфигурацию на ошибки
3. Убедитесь, что DNS настроен правильно
4. Проверьте права доступа к файлам
