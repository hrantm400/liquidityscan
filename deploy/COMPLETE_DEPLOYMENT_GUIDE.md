# Полное руководство по развертыванию домашней страницы на Contabo

**Один файл со всеми шагами от начала до конца**

---

## 📋 Содержание

1. [Проверка окружения сервера](#1-проверка-окружения-сервера)
2. [Локальная подготовка](#2-локальная-подготовка)
3. [Загрузка файлов на сервер через WinSCP](#3-загрузка-файлов-на-сервер-через-winscp)
4. [Настройка веб-сервера](#4-настройка-веб-сервера)
5. [Настройка DNS в Namecheap](#5-настройка-dns-в-namecheap)
6. [Проверка работы](#6-проверка-работы)
7. [Настройка Cloudflare (Рекомендуется - Бесплатный SSL + CDN)](#7-настройка-cloudflare-рекомендуется---бесплатный-ssl--cdn)
8. [Настройка SSL (HTTPS) - Альтернатива](#8-настройка-ssl-https---альтернатива-если-не-используете-cloudflare)
9. [Troubleshooting](#9-troubleshooting)

---

## 1. Проверка окружения сервера

**⚠️ КРИТИЧЕСКИ ВАЖНО:** Перед развертыванием нужно проверить, что установлено на сервере, чтобы не затронуть существующие сайты и боты!

### 1.1 Подключение к серверу через SSH

**Windows (PowerShell или CMD):**
```powershell
ssh root@YOUR_SERVER_IP
```
Замените `YOUR_SERVER_IP` на IP адрес вашего Contabo сервера.

**Или используйте PuTTY:**
1. Откройте PuTTY
2. Введите IP адрес сервера
3. Нажмите "Open"
4. Введите логин: `root`
5. Введите пароль

### 1.2 Проверка операционной системы

На сервере выполните:
```bash
cat /etc/os-release
```

Вы увидите что-то вроде:
```
NAME="Ubuntu"
VERSION="22.04.3 LTS (Jammy Jellyfish)"
```

**Запишите версию Ubuntu** - это может понадобиться.

### 1.3 Проверка установленного веб-сервера

**Проверка Nginx:**
```bash
which nginx
nginx -v
systemctl status nginx
```

Если Nginx установлен, вы увидите:
- Путь к nginx: `/usr/sbin/nginx`
- Версию: `nginx version: nginx/1.18.0`
- Статус: `active (running)`

**Примечание:** Команда `systemctl status nginx` покажет статус. Не копируйте текст `active (running)` как команду - это просто вывод команды!

**Проверка Apache:**
```bash
which apache2
apache2 -v
systemctl status apache2
```

Или для некоторых систем:
```bash
which httpd
httpd -v
systemctl status httpd
```

**⚠️ ВАЖНО:** Если Apache установлен, но сервис не найден (`Unit apache2.service could not be found`), это означает, что Apache установлен, но не используется. **Используйте Nginx для развертывания!**

**Запишите, какой веб-сервер установлен и активен** (Nginx / Apache / оба, но какой используется).

### 1.4 Проверка существующих доменов

**Если установлен Nginx:**
```bash
ls -la /etc/nginx/sites-enabled/
grep -h 'server_name' /etc/nginx/sites-enabled/* | grep -v '^#' | sort -u
```

**Если установлен Apache:**
```bash
ls -la /etc/apache2/sites-enabled/
grep -h 'ServerName\|ServerAlias' /etc/apache2/sites-enabled/* | grep -v '^#' | sort -u
```

**Запишите все существующие домены** - ваш новый домен НЕ должен совпадать с ними!

### 1.5 Проверка существующих директорий

```bash
ls -la /var/www/
```

**Запишите существующие директории** - вы будете использовать новую директорию `/var/www/liquidityscan.io`.

### 1.6 Проверка запущенных процессов (боты)

```bash
ps aux | grep -E 'python|node|java|telegram|bot' | grep -v grep
```

Это покажет запущенные боты и другие сервисы. **Убедитесь, что мы их не затронем!**

### 1.7 Итоги проверки

**На основе вашей проверки сервера:**

- ✅ Версия Ubuntu: **20.04.5 LTS (Focal Fossa)**
- ✅ Установленный веб-сервер: **Nginx (версия 1.18.0)** - активен и работает
- ✅ Apache: установлен, но не используется (сервис не найден)
- ✅ Существующие домены: 
  - `api.redditgoldmine.com`
  - `redditgoldmine.com`
  - `www.redditgoldmine.com`
- ✅ Существующие директории: 
  - `/var/www/html`
  - `/var/www/redditgoldmine`
- ✅ Запущенные процессы: Node.js сервер, Python боты, Java приложение

**✅ Вывод:** Используйте **Nginx** для развертывания нового домена. 

**Ваш домен:** `liquidityscan.io`  
**Директория на сервере:** `/var/www/liquidityscan.io`

---

## 2. Локальная подготовка

### 2.1 Переход в директорию frontend

Откройте терминал (PowerShell или CMD) и перейдите в проект:

```powershell
cd "C:\Users\hrant\Desktop\liquidityscan -MEGA\liquidityscan-web\frontend"
```

### 2.2 Установка зависимостей

**Если возникает ошибка с конфликтом зависимостей** (например, react-helmet-async требует старую версию React), используйте флаг `--legacy-peer-deps`:

```powershell
npm install --legacy-peer-deps
```

**Или обычная установка (если нет конфликтов):**
```powershell
npm install
```

Это может занять несколько минут. Дождитесь завершения.

**Примечание:** Флаг `--legacy-peer-deps` игнорирует конфликты версий зависимостей и позволяет установить пакеты, даже если есть несовместимости. Это безопасно для большинства случаев.

### 2.3 Сборка production версии

```powershell
npm run build
```

После успешной сборки вы увидите что-то вроде:
```
vite v5.x.x building for production...
✓ built in 15.23s
```

### 2.4 Проверка результата сборки

```powershell
dir dist
```

Вы должны увидеть:
- `index.html`
- `assets/` (директория с JS, CSS файлами)
- Другие файлы (robots.txt, sitemap.xml и т.д.)

**Путь к собранным файлам:** `C:\Users\hrant\Desktop\liquidityscan -MEGA\liquidityscan-web\frontend\dist\`

---

## 3. Загрузка файлов на сервер через WinSCP

### 3.1 Подключение к серверу в WinSCP

1. **Откройте WinSCP**
2. **Создайте новое подключение:**
   - **File protocol:** SFTP
   - **Host name:** IP адрес вашего сервера (например: `123.45.67.89`)
   - **Port number:** `22`
   - **User name:** `root`
   - **Password:** ваш пароль от сервера
   - Нажмите **"Save"** чтобы сохранить подключение
   - Нажмите **"Login"**

3. **При первом подключении** появится предупреждение о сертификате - нажмите **"Yes"**

### 3.2 Создание директории на сервере

В WinSCP:

1. **Перейдите в `/var/www/`** (в правой панели WinSCP)
2. **Создайте новую директорию:**
   - Правой кнопкой мыши в правой панели (сервер)
   - Выберите **"New"** → **"Directory"**
   - Введите имя: `liquidityscan.io`
   - Нажмите **OK**

**Или через терминал в WinSCP:**
1. Нажмите кнопку **"Terminal"** в WinSCP (или F9)
2. Выполните:
   ```bash
   sudo mkdir -p /var/www/liquidityscan.io
   sudo chown -R $USER:$USER /var/www/liquidityscan.io
   ```

### 3.3 Загрузка файлов

1. **В левой панели WinSCP** перейдите в:
   ```
   C:\Users\hrant\Desktop\liquidityscan -MEGA\liquidityscan-web\frontend\dist
   ```

2. **В правой панели WinSCP** перейдите в:
   ```
   /var/www/liquidityscan.io
   ```

3. **Выделите все файлы** в левой панели (Ctrl+A)

4. **Перетащите файлы** из левой панели в правую, или:
   - Правой кнопкой мыши на выделенных файлах
   - Выберите **"Upload"**

5. **Дождитесь завершения загрузки** - внизу WinSCP будет показан прогресс

### 3.4 Установка правильных прав доступа

**Через терминал WinSCP** (кнопка Terminal или F9):

```bash
sudo chown -R www-data:www-data /var/www/liquidityscan.io
sudo chmod -R 755 /var/www/liquidityscan.io
```

**Проверка:**
```bash
ls -la /var/www/liquidityscan.io
```

Вы должны увидеть файлы с правильными правами доступа.

---

## 4. Настройка веб-сервера

### 4.1 Определение веб-сервера

**На основе проверки вашего сервера:**
- ✅ **Apache (httpd)** - активен и работает на порту 80
- ✅ **cPanel EasyApache** - управляет конфигурацией Apache
- ⚠️ **Nginx** - установлен, но НЕ активен (не используется)

**✅ Используйте Apache для развертывания!** Переходите к разделу 4.3.

**⚠️ ВАЖНО:** У вас cPanel, который управляет Apache. Конфигурации можно добавить вручную, но рекомендуется использовать cPanel панель управления, если она доступна.

---

### 4.2 Настройка Nginx (НЕ ИСПОЛЬЗУЕТСЯ)

**⚠️ ПРОПУСТИТЕ ЭТОТ РАЗДЕЛ!** На вашем сервере используется Apache, а не Nginx.

---

### 4.3 Настройка Apache (cPanel EasyApache)

#### Шаг 4.3.1: Подготовка конфигурационного файла

**✅ Ваш домен:** `liquidityscan.io`

**✅ ГОТОВО:** Файл `apache-homepage.conf` уже обновлен с доменом `liquidityscan.io`!

**⚠️ ВАЖНО:** Убедитесь, что домен `liquidityscan.io` НЕ совпадает с существующими доменами:
- `api.redditgoldmine.com`
- `redditgoldmine.com`
- `www.redditgoldmine.com`

**Файл готов к использованию:** `C:\Users\hrant\Desktop\liquidityscan -MEGA\deploy\apache-homepage.conf`

#### Шаг 4.3.2: Определение места для конфигурации Apache

**Для cPanel EasyApache** конфигурации обычно находятся в:
- `/etc/apache2/conf.d/` (основное место - проверьте ваш httpd.conf!)
- `/usr/local/apache/conf/includes/` (может не подхватываться автоматически)

**⚠️ ВАЖНО:** Проверьте, какие директории включены в основной конфиг:

```bash
# Проверьте основной конфиг Apache
grep -i "Include" /usr/local/apache/conf/httpd.conf | grep -v "^#" | head -10
```

**На основе проверки вашего сервера:** Apache включает файлы из `/etc/apache2/conf.d/`, поэтому используйте этот путь!

#### Шаг 4.3.3: Загрузка конфигурации на сервер

**Через WinSCP:**

1. **В левой панели** откройте:
   ```
   C:\Users\hrant\Desktop\liquidityscan -MEGA\deploy\
   ```

2. **Найдите файл** `apache-homepage.conf` (уже с замененным доменом `liquidityscan.io`)

3. **В правой панели** перейдите в:
   ```
   /etc/apache2/conf.d/
   ```
   (это правильный путь для вашего сервера, проверенный в шаге 4.3.2)

4. **Перетащите файл** `apache-homepage.conf` в правую панель

5. **Переименуйте файл** на сервере:
   - Правой кнопкой на файле в правой панели
   - Выберите **"Rename"**
   - Введите: `liquidityscan.io.conf`

**Или через терминал WinSCP:**

```bash
# Создайте файл в правильной директории
sudo nano /etc/apache2/conf.d/liquidityscan.io.conf
```

Скопируйте содержимое из `apache-homepage.conf` (где все `yourdomain.com` уже заменены на `liquidityscan.io`) и вставьте в nano (Ctrl+Shift+V), затем:
- Сохраните: Ctrl+O, Enter
- Выйдите: Ctrl+X

#### Шаг 4.3.4: Проверка конфигурации Apache

**⚠️ ВАЖНО:** Перед проверкой убедитесь, что:
1. Файл называется `liquidityscan.io.conf` (С расширением .conf)
2. Файл находится в `/etc/apache2/conf.d/` (правильный путь для вашего сервера)
3. Файл содержит правильный домен `liquidityscan.io`

**Проверка содержимого файла:**
```bash
# Убедитесь, что файл содержит правильную конфигурацию
head -20 /etc/apache2/conf.d/liquidityscan.io.conf
```

**Проверка конфигурации Apache:**

**Через терминал WinSCP:**

```bash
# Для cPanel EasyApache используйте httpd вместо apache2ctl
sudo httpd -t
```

**Если все правильно**, вы увидите:
```
Syntax OK
```

**Если есть ошибки:**

**⚠️ ВАЖНО:** Ошибка может быть в существующих файлах, а не в вашем новом файле!

**Если ошибка указывает на другой файл**, проверьте этот файл:

```bash
# Проверьте проблемный файл (замените на имя из ошибки)
head -5 /usr/local/apache/conf/includes/проблемный_файл.conf
```

**Если ошибка в вашем файле:**
- Проверьте содержимое: `cat /etc/apache2/conf.d/liquidityscan.io.conf`
- Убедитесь, что нет лишних символов или неправильных директив
- Убедитесь, что все пути правильные (DocumentRoot должен указывать на `/var/www/liquidityscan.io`)

#### Шаг 4.3.5: Перезагрузка Apache

**⚠️ ВАЖНО:** В cPanel конфигурации Apache обычно автоматически подхватываются, но для уверенности перезагрузите:

```bash
# Перезагрузите Apache (httpd)
sudo systemctl reload httpd
```

**Или перезапустите:**
```bash
sudo systemctl restart httpd
```

**Проверка статуса:**
```bash
sudo systemctl status httpd
```

Должно быть: `active (running)`

**Проверка, что конфигурация загружена:**

**Сначала проверьте, что файл существует:**
```bash
ls -la /etc/apache2/conf.d/liquidityscan.io.conf
```

**Если файл НЕ существует**, загрузите его (см. шаг 4.3.3).

**⚠️ Если файл находится в `/usr/local/apache/conf/includes/`, переместите его:**
```bash
sudo mv /usr/local/apache/conf/includes/liquidityscan.io.conf /etc/apache2/conf.d/liquidityscan.io.conf
```

**Затем проверьте, что Apache видит ваш VirtualHost:**
```bash
sudo httpd -S | grep liquidityscan.io
```

**⚠️ ВАЖНО для cPanel:** Если домен не показывается, возможно, cPanel не подхватывает файлы из `/usr/local/apache/conf/includes/` автоматически. 

**Альтернативный вариант для cPanel:**

1. **Проверьте, где cPanel хранит конфигурации доменов:**
   ```bash
   ls -la /usr/local/apache/conf/userdata/
   find /usr/local/apache/conf -name "*liquidityscan*" 2>/dev/null
   ```

2. **Или используйте cPanel панель управления** для добавления домена (если доступна)

3. **Или создайте файл в другом месте:**
   ```bash
   # Проверьте, какие директории включены в основной конфиг
   grep -i "Include" /usr/local/apache/conf/httpd.conf | grep -v "^#" | head -10
   ```

**Если файл существует, но домен не показывается:**
- Проверьте синтаксис файла: `sudo httpd -t`
- Убедитесь, что файл содержит правильный VirtualHost
- Возможно, нужно перезапустить Apache (не reload): `sudo systemctl restart httpd`

#### Шаг 4.3.6: ⚠️ РЕШЕНИЕ ПРОБЛЕМЫ cPanel (если curl возвращает defaultwebpage.cgi)

**Проблема:** Если при проверке `curl -H "Host: liquidityscan.io" http://localhost` вы видите редирект на `/cgi-sys/defaultwebpage.cgi`, это означает, что cPanel перехватывает запросы.

**ШАГ 1: Диагностика проблемы**

**Проверьте все VirtualHost на порту 80:**
```bash
sudo httpd -S | grep ":80"
```

**Проверьте, какой VirtualHost обрабатывает запросы для liquidityscan.io:**
```bash
sudo httpd -S | grep -A 5 liquidityscan.io
```

**Проверьте конфигурации cPanel:**
```bash
# Найдите все конфигурации cPanel
find /usr/local/apache/conf -name "*defaultwebpage*" 2>/dev/null
find /usr/local/apache/conf -name "*catchall*" 2>/dev/null
ls -la /usr/local/apache/conf/userdata/
```

**Проверьте основной конфиг Apache на наличие catch-all VirtualHost:**
```bash
grep -r "defaultwebpage\|catchall" /usr/local/apache/conf/ 2>/dev/null | head -20
```

**ШАГ 2: Решение проблемы**

**Решение 1: Изменить порядок VirtualHost (приоритет)**

**Если ваш VirtualHost не первый в списке для порта 80, переименуйте файл так, чтобы он загружался последним:**
```bash
# Переименуйте файл, чтобы он загружался последним (z- в начале имени)
sudo mv /etc/apache2/conf.d/liquidityscan.io.conf /etc/apache2/conf.d/z-liquidityscan.io.conf
```

**Или добавьте приоритет в конфигурацию:**
```bash
# Отредактируйте файл
sudo nano /etc/apache2/conf.d/liquidityscan.io.conf
```

**Измените первую строку VirtualHost на:**
```apache
<VirtualHost *:80>
    # Добавьте это в самое начало, чтобы VirtualHost имел приоритет
    ServerName liquidityscan.io
    ServerAlias www.liquidityscan.io
```

**Решение 2: Добавить домен в cPanel (если доступна панель управления)**

1. Войдите в cPanel панель управления
2. Найдите раздел "Addon Domains" или "Subdomains"
3. Добавьте домен `liquidityscan.io`
4. Укажите DocumentRoot: `/var/www/liquidityscan.io`

**Решение 3: Отключить cPanel перехват для этого домена**

**Проверьте, есть ли конфигурация cPanel для этого домена:**
```bash
find /usr/local/apache/conf -name "*liquidityscan*" 2>/dev/null
ls -la /usr/local/apache/conf/userdata/
```

**Если есть файлы cPanel для этого домена, временно переименуйте их:**
```bash
# Найдите и переименуйте файлы cPanel (если есть)
sudo find /usr/local/apache/conf -name "*liquidityscan*" -exec mv {} {}.bak \;
```

**Решение 4: Использовать директиву ServerAlias с IP**

**Добавьте в ваш VirtualHost:**
```apache
<VirtualHost *:80>
    ServerName liquidityscan.io
    ServerAlias www.liquidityscan.io 185.216.75.174
    # ... остальная конфигурация
</VirtualHost>
```

**Решение 5: ⚠️ САМОЕ ЭФФЕКТИВНОЕ - Добавить домен в cPanel**

**Если у вас есть доступ к cPanel панели управления:**

1. Войдите в cPanel (обычно: `http://185.216.75.174:2083` или `https://185.216.75.174:2087`)
2. Найдите раздел **"Addon Domains"** или **"Subdomains"**
3. Добавьте домен `liquidityscan.io`
4. Укажите DocumentRoot: `/var/www/liquidityscan.io`
5. Сохраните

**После добавления домена в cPanel, он автоматически создаст правильную конфигурацию и ваш VirtualHost будет работать!**

**Решение 6: Отключить catch-all VirtualHost cPanel (если нет доступа к cPanel)**

**Найдите и временно отключите catch-all VirtualHost:**
```bash
# Найдите файлы с catch-all конфигурацией
grep -r "defaultwebpage\|catchall" /usr/local/apache/conf/ 2>/dev/null

# Если найдете файл, временно переименуйте его
# Например:
sudo mv /usr/local/apache/conf/includes/catchall.conf /usr/local/apache/conf/includes/catchall.conf.bak
```

**⚠️ ВНИМАНИЕ:** Это может повлиять на другие домены на сервере! Используйте только если уверены!

**Решение 7: Использовать более специфичный VirtualHost с приоритетом**

**Отредактируйте ваш конфигурационный файл:**
```bash
sudo nano /etc/apache2/conf.d/z-liquidityscan.io.conf
```

**Убедитесь, что VirtualHost выглядит так (ServerName должен быть ПЕРВЫМ):**
```apache
<VirtualHost *:80>
    # ServerName должен быть ПЕРВЫМ - это дает приоритет
    ServerName liquidityscan.io
    ServerAlias www.liquidityscan.io
    
    DocumentRoot /var/www/liquidityscan.io
    
    <Directory /var/www/liquidityscan.io>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted
        
        RewriteEngine On
        RewriteBase /
        RewriteRule ^index\.html$ - [L]
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteCond %{REQUEST_FILENAME} !-d
        RewriteRule . /index.html [L]
    </Directory>
    
    ErrorLog /etc/apache2/logs/liquidityscan.io_error.log
    CustomLog /etc/apache2/logs/liquidityscan.io_access.log combined
</VirtualHost>
```

**После изменений:**
```bash
# Проверьте конфигурацию
sudo httpd -t

# Перезапустите Apache (не reload!)
sudo systemctl restart httpd

# Проверьте снова
curl -H "Host: liquidityscan.io" http://localhost
```

**Должен вернуться HTML вашего сайта, а не редирект на defaultwebpage.cgi!**

**Если все еще не работает, проверьте логи:**
```bash
# Проверьте логи ошибок
sudo tail -20 /etc/apache2/logs/liquidityscan.io_error.log

# Проверьте логи доступа
sudo tail -20 /etc/apache2/logs/liquidityscan.io_access.log
```

**Решение 8: Проверить основной конфиг httpd.conf на catch-all**

**Проверьте основной конфиг на наличие default или catch-all VirtualHost:**
```bash
# Проверьте основной конфиг
grep -A 10 "defaultwebpage\|catchall\|_default_" /etc/apache2/conf/httpd.conf | head -30

# Проверьте, есть ли default VirtualHost
grep -B 5 -A 10 "VirtualHost.*:80" /etc/apache2/conf/httpd.conf | grep -A 10 "ServerName\|default"
```

**Если найден default VirtualHost, который перехватывает запросы, нужно либо:**
1. Отключить его (если не нужен)
2. Или сделать ваш VirtualHost более специфичным

**Решение 9: Проверить содержимое вашего конфига и убедиться, что он правильный**

**Проверьте содержимое вашего конфига:**
```bash
cat /etc/apache2/conf.d/z-liquidityscan.io.conf
```

**Убедитесь, что:**
- ServerName указан ПЕРВЫМ в VirtualHost
- DocumentRoot указывает на правильную директорию
- Нет синтаксических ошибок

**Решение 10: ⚠️ САМОЕ БЕЗОПАСНОЕ - Использовать конкретный IP вместо *:80**

**Проблема:** VirtualHost с `*:80` может загружаться после более специфичного VirtualHost с конкретным IP `185.216.75.174:80` из основного конфига.

**Решение:** Измените VirtualHost, чтобы использовать конкретный IP адрес вместо `*:80`. Это безопасно и не затронет другие сайты!

**Отредактируйте конфиг:**
```bash
sudo nano /etc/apache2/conf.d/z-liquidityscan.io.conf
```

**Измените первую строку VirtualHost с:**
```apache
<VirtualHost *:80>
```

**На:**
```apache
<VirtualHost 185.216.75.174:80>
```

**Полный VirtualHost должен выглядеть так:**
```apache
<VirtualHost 185.216.75.174:80>
    # ServerName должен быть ПЕРВЫМ - это дает приоритет
    ServerName liquidityscan.io
    ServerAlias www.liquidityscan.io
    
    # Root directory for static files
    DocumentRoot /var/www/liquidityscan.io
    
    # Directory configuration
    <Directory /var/www/liquidityscan.io>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted
        
        # SPA routing support - redirect all requests to index.html
        RewriteEngine On
        RewriteBase /
        RewriteRule ^index\.html$ - [L]
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteCond %{REQUEST_FILENAME} !-d
        RewriteRule . /index.html [L]
    </Directory>
    
    # Logging
    ErrorLog /etc/apache2/logs/liquidityscan.io_error.log
    CustomLog /etc/apache2/logs/liquidityscan.io_access.log combined
    
    # Security headers
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    
    # Enable compression
    <IfModule mod_deflate.c>
        AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
    </IfModule>
    
    # Cache static assets
    <IfModule mod_expires.c>
        ExpiresActive On
        ExpiresByType image/jpg "access plus 1 year"
        ExpiresByType image/jpeg "access plus 1 year"
        ExpiresByType image/gif "access plus 1 year"
        ExpiresByType image/png "access plus 1 year"
        ExpiresByType image/svg+xml "access plus 1 year"
        ExpiresByType text/css "access plus 1 year"
        ExpiresByType application/javascript "access plus 1 year"
        ExpiresByType application/x-javascript "access plus 1 year"
        ExpiresByType font/woff "access plus 1 year"
        ExpiresByType font/woff2 "access plus 1 year"
        ExpiresByType application/font-woff "access plus 1 year"
        ExpiresByType application/font-woff2 "access plus 1 year"
    </IfModule>
    
    # Deny access to hidden files
    <FilesMatch "^\.">
        Require all denied
    </FilesMatch>
</VirtualHost>
```

**После изменений:**
```bash
# Проверьте конфигурацию
sudo httpd -t

# Перезапустите Apache (не reload!)
sudo systemctl restart httpd

# Проверьте снова
curl -H "Host: liquidityscan.io" http://localhost
```

**⚠️ БЕЗОПАСНО:** Это изменение не затронет другие сайты, так как:
- Ваш VirtualHost использует конкретный IP `185.216.75.174:80`
- ServerName `liquidityscan.io` специфичен для вашего домена
- Другие домены (redditgoldmine.com и т.д.) имеют свои ServerName и не будут затронуты

**Если все еще не работает после изменения на `185.216.75.174:80`:**

**Проверьте, что изменения применены:**
```bash
# Проверьте содержимое файла
grep "VirtualHost" /etc/apache2/conf.d/z-liquidityscan.io.conf | head -1
```

**Должно быть:** `<VirtualHost 185.216.75.174:80>`

**Проверьте, какой VirtualHost Apache выбирает:**
```bash
# Проверьте все VirtualHost для вашего IP
sudo httpd -S | grep "185.216.75.174:80"

# Проверьте, какой VirtualHost будет использован для liquidityscan.io
sudo httpd -S | grep -B 2 -A 2 liquidityscan
```

**Проблема может быть в том, что VirtualHost в основном конфиге `httpd.conf` с IP `185.216.75.174:80` и ServerName `vmi1229446.contaboserver.net` имеет приоритет.**

**Решение 11: Проверить и изменить порядок загрузки конфигов**

**Проверьте, в каком порядке загружаются конфиги:**
```bash
# Проверьте, какие Include директивы есть в основном конфиге
grep -i "Include" /etc/apache2/conf/httpd.conf | grep -v "^#" | tail -10
```

**Если конфиги из `/etc/apache2/conf.d/` загружаются ДО основного конфига, то VirtualHost из основного конфига может перезаписать ваш.**

**Решение 12: Использовать более высокий приоритет через имя файла**

**Переименуйте файл так, чтобы он загружался ПОСЛЕ основного конфига:**
```bash
# Если конфиги загружаются в алфавитном порядке, используйте имя, которое загрузится последним
sudo mv /etc/apache2/conf.d/z-liquidityscan.io.conf /etc/apache2/conf.d/zzz-liquidityscan.io.conf
```

**Или проверьте, есть ли директория, которая загружается ПОСЛЕ основного конфига:**
```bash
# Проверьте все Include директивы
grep -i "Include" /etc/apache2/conf/httpd.conf | grep -v "^#"
```

**Решение 13: Добавить домен в систему управления (если это cPanel или подобное)**

**Если у вас есть доступ к панели управления (cPanel, Plesk и т.д.), добавьте домен через панель - это самый надежный способ!**

**Решение 14: ⚠️ КРИТИЧЕСКИ ВАЖНО - Найти RewriteRule, который перехватывает запросы**

**Проблема:** Apache видит ваш VirtualHost, но все равно возвращает редирект на defaultwebpage.cgi. Это означает, что где-то есть RewriteRule, который перехватывает запросы.

**Проверьте основной конфиг и другие конфиги на наличие RewriteRule для defaultwebpage:**
```bash
# Найдите все упоминания defaultwebpage в конфигах
grep -r "defaultwebpage" /etc/apache2/conf/ 2>/dev/null

# Найдите RewriteRule, которые могут перехватывать запросы
grep -r "RewriteRule.*defaultwebpage\|RewriteCond.*defaultwebpage" /etc/apache2/conf/ 2>/dev/null

# Проверьте основной конфиг на наличие RewriteRule
grep -A 5 -B 5 "RewriteRule\|RewriteCond" /etc/apache2/conf/httpd.conf | grep -i "defaultwebpage\|catchall" | head -20
```

**Проверьте, есть ли в основном конфиге RewriteRule для неизвестных доменов:**
```bash
# Проверьте VirtualHost с IP 185.216.75.174:80 в основном конфиге
grep -A 30 "VirtualHost 185.216.75.174:80" /etc/apache2/conf/httpd.conf | grep -A 20 "RewriteRule\|RewriteCond"
```

**Если найден RewriteRule, который перехватывает запросы, нужно либо:**
1. Отключить его для вашего домена
2. Или добавить исключение для liquidityscan.io

**Решение 15: Добавить RewriteCond для исключения вашего домена**

**Если в основном конфиге есть RewriteRule, который перехватывает запросы, добавьте в ваш VirtualHost исключение:**

**Отредактируйте ваш конфиг:**
```bash
sudo nano /etc/apache2/conf.d/zzz-liquidityscan.io.conf
```

**Добавьте в начало VirtualHost (после ServerName, но перед DocumentRoot):**
```apache
<VirtualHost 185.216.75.174:80>
    ServerName liquidityscan.io
    ServerAlias www.liquidityscan.io
    
    # Отключить RewriteEngine из глобальных настроек для этого домена
    RewriteEngine On
    RewriteOptions InheritBefore
    
    # Если есть глобальные RewriteRule, они будут применены, но мы можем их переопределить
    # Или просто не наследовать RewriteOptions
    # RewriteOptions Inherit
    
    DocumentRoot /var/www/liquidityscan.io
    # ... остальное
</VirtualHost>
```

**Или попробуйте отключить наследование RewriteOptions:**
```apache
<VirtualHost 185.216.75.174:80>
    ServerName liquidityscan.io
    ServerAlias www.liquidityscan.io
    
    # Не наследовать RewriteOptions из глобальных настроек
    RewriteEngine On
    RewriteOptions None
    
    DocumentRoot /var/www/liquidityscan.io
    # ... остальное
</VirtualHost>
```

**Решение 16: Проверить, есть ли скрипт defaultwebpage.cgi и как он вызывается**

**Проверьте, где находится скрипт defaultwebpage.cgi:**
```bash
# Найдите скрипт defaultwebpage.cgi
find / -name "defaultwebpage.cgi" 2>/dev/null

# Проверьте, есть ли .htaccess файлы, которые могут перехватывать запросы
find /var/www -name ".htaccess" -exec grep -l "defaultwebpage" {} \; 2>/dev/null

# Проверьте основной DocumentRoot на наличие .htaccess
cat /var/www/html/.htaccess 2>/dev/null
```

**Проверьте, может быть проблема в том, что VirtualHost в основном конфиге имеет RewriteOptions Inherit, который наследует правила:**
```bash
# Проверьте полный VirtualHost с IP 185.216.75.174:80
grep -A 100 "VirtualHost 185.216.75.174:80" /etc/apache2/conf/httpd.conf | head -100
```

**Решение 17: Отключить наследование RewriteOptions в вашем VirtualHost**

**Попробуйте добавить в ваш VirtualHost явное отключение наследования:**
```bash
sudo nano /etc/apache2/conf.d/zzz-liquidityscan.io.conf
```

**Измените VirtualHost так (добавьте RewriteOptions None в самое начало, сразу после открытия тега):**
```apache
<VirtualHost 185.216.75.174:80>
    # Отключить наследование RewriteOptions из глобальных настроек
    RewriteEngine On
    RewriteOptions None
    
    ServerName liquidityscan.io
    ServerAlias www.liquidityscan.io
    
    DocumentRoot /var/www/liquidityscan.io
    
    <Directory /var/www/liquidityscan.io>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted
        
        # SPA routing support - redirect all requests to index.html
        RewriteEngine On
        RewriteBase /
        RewriteRule ^index\.html$ - [L]
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteCond %{REQUEST_FILENAME} !-d
        RewriteRule . /index.html [L]
    </Directory>
    
    # ... остальное
</VirtualHost>
```

**После изменений:**
```bash
# Проверьте конфигурацию
sudo httpd -t

# Перезапустите Apache
sudo systemctl restart httpd

# Проверьте снова
curl -H "Host: liquidityscan.io" http://localhost
```

---

**✅ Готово!** Конфигурация Apache создана и активирована. Переходите к разделу 5 (Настройка DNS).

---

## 5. Настройка DNS в Namecheap

### 5.1 Вход в панель управления Namecheap

1. Откройте браузер и перейдите на: https://www.namecheap.com
2. Войдите в ваш аккаунт
3. Перейдите в **"Domain List"**

### 5.2 Проверка Nameservers (КРИТИЧЕСКИ ВАЖНО!)

**⚠️ ВАЖНО:** Если в браузере показывается страница Hostinger или другого хостинга вместо вашего сайта, это означает, что nameservers домена все еще указывают на другого провайдера!

**Проверка nameservers:**

1. В Namecheap перейдите в **"Domain List"**
2. Найдите домен `liquidityscan.io`
3. Нажмите **"Manage"**
4. Перейдите на вкладку **"Domain"**
5. Проверьте секцию **"Nameservers"**

**Должно быть:**
- **"Namecheap BasicDNS"** или **"Namecheap Web Hosting DNS"** (для использования Advanced DNS)
- **НЕ должно быть:** Hostinger nameservers или других провайдеров!

**Если nameservers указывают на Hostinger или другого провайдера:**

1. Выберите **"Namecheap BasicDNS"** или **"Custom DNS"**
2. Если выбран "Custom DNS", измените на **"Namecheap BasicDNS"**
3. Нажмите **"Save"**
4. Подождите 5-10 минут для обновления nameservers

**⚠️ КРИТИЧЕСКИ ВАЖНО:** Пока nameservers указывают на Hostinger, ваши A-записи в Advanced DNS не будут работать! Сначала измените nameservers на Namecheap!

### 5.3 Настройка Advanced DNS

1. После того как nameservers изменены на Namecheap, перейдите на вкладку **"Advanced DNS"**

### 5.3.1 ⚠️ ВАЖНО: Удаление старых A-записей

**Перед добавлением новых записей, проверьте и удалите старые A-записи!**

1. В секции **"Host Records"** найдите все записи типа **"A Record"** для:
   - Host: `@` (основной домен)
   - Host: `www` (поддомен www)

2. **Если есть старые A-записи с другими IP адресами** (не `185.216.75.174`):
   - Нажмите на **красный крестик** (удалить) рядом с каждой старой записью
   - Удалите ВСЕ старые A-записи для `@` и `www`

3. **Оставьте только записи типа:**
   - CNAME записи (если есть)
   - MX записи (для email)
   - TXT записи (для верификации)
   - **НО удалите все старые A-записи!**

**⚠️ КРИТИЧЕСКИ ВАЖНО:** Если вы видите в nslookup IP адреса `77.37.53.94` или `91.108.98.216` вместо `185.216.75.174`, значит в Namecheap есть старые A-записи, которые нужно удалить!

### 5.4 Добавление A-записи для основного домена

**✅ Ваш IP адрес сервера:** `185.216.75.174`

1. В секции **"Host Records"** нажмите **"Add New Record"**
2. Заполните:
   - **Type:** `A Record`
   - **Host:** `@` (символ @ означает основной домен)
   - **Value:** `185.216.75.174` ⬅️ **ВАШ IP АДРЕС**
   - **TTL:** `Automatic` (или `1800` секунд)
3. Нажмите **"Save"** (зеленая галочка)

### 5.5 Добавление A-записи для поддомена www

1. Снова нажмите **"Add New Record"**
2. Заполните:
   - **Type:** `A Record`
   - **Host:** `www`
   - **Value:** `185.216.75.174` ⬅️ **ТОТ ЖЕ IP АДРЕС**
   - **TTL:** `Automatic`
3. Нажмите **"Save"**

### 5.6 Сохранение изменений

Нажмите кнопку **"Save All Changes"** (зеленая галочка вверху страницы)

### 5.7 Ожидание распространения DNS

DNS изменения могут занять:
- **Минимум:** 5-10 минут
- **Обычно:** 30-60 минут
- **Максимум:** до 48 часов

**Проверка распространения DNS:**

На вашем компьютере (PowerShell):
```powershell
nslookup liquidityscan.io
```

Или:
```powershell
ping liquidityscan.io
```

Должен вернуться IP адрес вашего сервера: `185.216.75.174`

### 5.8 ⚠️ Проблема: DNS показывает неправильный IP адрес

**Если после настройки DNS вы видите другие IP адреса** (например, `77.37.53.94` или `91.108.98.216` вместо `185.216.75.174`):

**Решение:**

1. **Вернитесь в Namecheap → Advanced DNS**

2. **Проверьте все A-записи в секции "Host Records":**
   - Должна быть ТОЛЬКО ОДНА A-запись для Host `@` с Value `185.216.75.174`
   - Должна быть ТОЛЬКО ОДНА A-запись для Host `www` с Value `185.216.75.174`
   - **Удалите ВСЕ остальные A-записи** для `@` и `www` с другими IP адресами!

3. **Если вы видите несколько A-записей для одного Host:**
   - Удалите все старые записи (красный крестик)
   - Оставьте только одну правильную запись с IP `185.216.75.174`

4. **Нажмите "Save All Changes"** (зеленая галочка вверху)

5. **Подождите 5-10 минут** и проверьте снова:
   ```powershell
   nslookup liquidityscan.io
   ```

6. **Очистите DNS кэш на вашем компьютере:**
   ```powershell
   ipconfig /flushdns
   ```

7. **Проверьте через Google DNS (обход кэша провайдера):**
   ```powershell
   nslookup liquidityscan.io 8.8.8.8
   ```
   Это проверит DNS напрямую через Google DNS, минуя кэш вашего провайдера.

8. **Проверьте только IPv4 (A-записи):**
   ```powershell
   nslookup -type=A liquidityscan.io 8.8.8.8
   ```
   Это покажет только A-записи (IPv4), без IPv6 адресов.

9. **Если все еще показывает неправильные IP:**
   - Подождите еще 10-15 минут (DNS изменения могут занять время)
   - Проверьте в Namecheap, что нет других A-записей (может быть скрыто в других секциях)
   - Проверьте, нет ли AAAA записей (IPv6), которые могут конфликтовать

**Должен вернуться IP:** `185.216.75.174`

---

## 6. Проверка работы

### 6.1 Проверка DNS

**На вашем компьютере:**

```powershell
nslookup liquidityscan.io
```

Должен вернуться IP адрес вашего сервера.

### 6.2 Проверка веб-сервера на сервере

**Через терминал WinSCP (SSH):**

**⚠️ ВАЖНО:** "Route not found" при обращении по IP адресу - это нормально! Apache настроен на домен, а не на IP.

**Проверка конфигурации Apache:**
```bash
# Проверьте, что Apache видит ваш VirtualHost
sudo httpd -S | grep liquidityscan.io
```

**Проверка через домен (локально на сервере):**
```bash
# Проверьте, что сайт работает через домен
curl -H "Host: liquidityscan.io" http://localhost
```

**Проверка файлов:**
```bash
# Убедитесь, что файлы находятся в правильной директории
ls -la /var/www/liquidityscan.io/
```

Должны быть файлы: `index.html`, `assets/` и другие.

### 6.3 Проверка в браузере

**Вариант 1: Через домен (если DNS уже распространился):**

1. Откройте браузер
2. Перейдите по адресу: `http://liquidityscan.io`
3. Должна загрузиться ваша домашняя страница

**Вариант 2: Временная проверка через hosts файл (если DNS еще не распространился):**

Если DNS еще не распространился, можно временно добавить запись в hosts файл для проверки:

**Windows (от имени администратора):**

1. Откройте Notepad от имени администратора
2. Откройте файл: `C:\Windows\System32\drivers\etc\hosts`
3. Добавьте строку в конец файла:
   ```
   185.216.75.174    liquidityscan.io
   185.216.75.174    www.liquidityscan.io
   ```
4. Сохраните файл
5. Откройте браузер и перейдите: `http://liquidityscan.io`

**⚠️ ВАЖНО:** После того как DNS распространится, удалите эти строки из hosts файла!

**Проверка через Google DNS (обход кэша):**
```powershell
# Проверьте через Google DNS
nslookup liquidityscan.io 8.8.8.8
```

Если возвращается `185.216.75.174`, то DNS уже работает и можно открыть сайт через домен.

### 6.4 Проверка существующих сервисов

**⚠️ ВАЖНО:** Убедитесь, что существующие сайты и боты все еще работают!

1. **Проверьте существующие сайты** - откройте их в браузере
2. **Проверьте боты** - проверьте их логи или статус

---

## 7. Настройка Cloudflare (Рекомендуется - Бесплатный SSL + CDN)

**✅ Преимущества Cloudflare:**
- 🆓 **Бесплатный SSL сертификат** (автоматически)
- 🚀 **CDN** - ускорение загрузки сайта
- 🛡️ **Защита от DDoS** атак
- 📊 **Аналитика** трафика
- 🔒 **Автоматическое шифрование** трафика

### 7.1 Создание аккаунта Cloudflare

1. Откройте: https://www.cloudflare.com
2. Нажмите **"Sign Up"** (Регистрация)
3. Создайте бесплатный аккаунт
4. Войдите в аккаунт

### 7.2 Добавление домена в Cloudflare

1. В панели Cloudflare нажмите **"Add a Site"** (Добавить сайт)
2. Введите домен: `liquidityscan.io`
3. Нажмите **"Add site"**
4. Выберите план: **Free** (бесплатный) - нажмите **"Continue"**
5. Cloudflare просканирует ваши DNS записи

### 7.3 Изменение Nameservers в Namecheap

**⚠️ ВАЖНО:** После добавления домена в Cloudflare, вам нужно изменить nameservers в Namecheap.

1. В Cloudflare после добавления домена вы увидите **2 nameservers**, например:
   ```
   dante.ns.cloudflare.com
   lola.ns.cloudflare.com
   ```
   **Запишите их!**

2. Откройте Namecheap → Domain List → Manage для `liquidityscan.io`

3. Перейдите на вкладку **"Domain"**

4. В секции **"Nameservers"** выберите **"Custom DNS"**

5. Введите nameservers от Cloudflare:
   - Первый nameserver: `dante.ns.cloudflare.com` (или тот, что показал Cloudflare)
   - Второй nameserver: `lola.ns.cloudflare.com` (или тот, что показал Cloudflare)

6. Нажмите **"Save"** (зеленая галочка)

7. **Подождите 5-30 минут** для обновления nameservers

### 7.4 Настройка DNS записей в Cloudflare

**После того как nameservers обновятся:**

1. В Cloudflare перейдите в ваш домен `liquidityscan.io`

2. Перейдите в **"DNS"** → **"Records"**

3. **Удалите старые записи** (если есть)

4. **Добавьте A-запись для основного домена:**
   - Нажмите **"Add record"**
   - **Type:** `A`
   - **Name:** `@` (или оставьте пустым)
   - **IPv4 address:** `185.216.75.174` ⬅️ **ВАШ IP СЕРВЕРА**
   - **Proxy status:** 🟠 **Proxied** (включено - это важно для SSL!)
   - **TTL:** `Auto`
   - Нажмите **"Save"**

5. **Добавьте A-запись для www:**
   - Нажмите **"Add record"**
   - **Type:** `A`
   - **Name:** `www`
   - **IPv4 address:** `185.216.75.174` ⬅️ **ТОТ ЖЕ IP**
   - **Proxy status:** 🟠 **Proxied** (включено!)
   - **TTL:** `Auto`
   - Нажмите **"Save"**

**⚠️ КРИТИЧЕСКИ ВАЖНО:** Proxy status должен быть **🟠 Proxied** (включено), а не **⚪ DNS only**! Это необходимо для работы SSL и CDN!

**✅ Как это работает:**

1. **В Cloudflare вы указываете IP вашего сервера:** `185.216.75.174`
   - Это нужно, чтобы Cloudflare знал, куда отправлять запросы

2. **Cloudflare проксирует трафик:**
   - Пользователь → Cloudflare (IP Cloudflare) → Ваш сервер (185.216.75.174)
   - Для пользователей виден IP Cloudflare, а не ваш серверный IP
   - Это **безопаснее** - скрывает реальный IP вашего сервера

3. **SSL работает автоматически:**
   - Cloudflare предоставляет SSL сертификат
   - Трафик между Cloudflare и пользователем зашифрован
   - Трафик между Cloudflare и вашим сервером тоже зашифрован (если настроен)

4. **CDN ускоряет сайт:**
   - Статические файлы кэшируются на серверах Cloudflare по всему миру
   - Пользователи получают файлы с ближайшего сервера Cloudflare

**🔒 Безопасность:**
- Реальный IP вашего сервера (`185.216.75.174`) **скрыт** от пользователей
- Виден только IP Cloudflare
- Это защищает ваш сервер от прямых атак

### 7.5 Настройка SSL в Cloudflare

**SSL включается автоматически!**

1. В Cloudflare перейдите в **"SSL/TLS"**

2. **SSL/TLS encryption mode:** выберите **"Full"** или **"Full (strict)"**
   - **Full** - работает с самоподписанными сертификатами на сервере
   - **Full (strict)** - требует валидный сертификат на сервере (рекомендуется после настройки Let's Encrypt)

3. **Always Use HTTPS:** включите (рекомендуется)
   - Это автоматически перенаправит HTTP на HTTPS

4. **Automatic HTTPS Rewrites:** включите (рекомендуется)
   - Автоматически переписывает HTTP ссылки на HTTPS

### 7.6 Ожидание активации SSL

**SSL активируется автоматически в течение 5-15 минут!**

Проверка:
1. Откройте в браузере: `https://liquidityscan.io`
2. Должен быть зеленый замочек 🔒
3. Сайт должен работать по HTTPS

### 7.7 Дополнительные настройки Cloudflare (Опционально)

**Speed (Скорость):**
- **Auto Minify:** включите для JS, CSS, HTML (уменьшает размер файлов)
- **Brotli:** включите (лучшее сжатие)

**Caching (Кэширование):**
- **Caching Level:** Standard
- **Browser Cache TTL:** 4 hours (или больше)

**Security (Безопасность):**
- **Security Level:** Medium (или High)
- **Bot Fight Mode:** включите (бесплатно)

### 7.8 Проверка работы через Cloudflare

**После настройки:**

1. **Проверьте DNS:**
   ```powershell
   nslookup liquidityscan.io
   ```
   Должен вернуться IP Cloudflare (не ваш серверный IP), если Proxy включен.

2. **Проверьте SSL:**
   - Откройте: `https://liquidityscan.io`
   - Должен быть зеленый замочек 🔒

3. **Проверьте скорость:**
   - Cloudflare автоматически кэширует статические файлы
   - Сайт должен загружаться быстрее

### 7.9 ⚠️ Проблема: Сайт открывается с телефона, но не с компьютера

**Это проблема DNS кэша на вашем компьютере!**

**Решение:**

1. **Очистите DNS кэш на Windows:**
   ```powershell
   ipconfig /flushdns
   ```

2. **Перезапустите браузер** (полностью закройте и откройте снова)

3. **Попробуйте другой браузер** (Chrome, Firefox, Edge)

4. **Используйте режим инкогнито** (Ctrl+Shift+N)

5. **Проверьте через Google DNS:**
   ```powershell
   nslookup liquidityscan.io 8.8.8.8
   ```

6. **Если все еще не работает, подождите 10-30 минут** - DNS кэш обновится автоматически

**После настройки Cloudflare эта проблема должна исчезнуть!**

---

## 8. Настройка SSL (HTTPS) - Альтернатива (если не используете Cloudflare)

После того как сайт заработает по HTTP, рекомендуется настроить HTTPS.

### 7.1 Установка Certbot

**На сервере через терминал WinSCP:**

**Для Nginx:**
```bash
sudo apt update
sudo apt install certbot python3-certbot-nginx -y
```

### 7.2 Получение SSL сертификата

**Для Nginx:**
```bash
sudo certbot --nginx -d liquidityscan.io -d www.liquidityscan.io
```

Следуйте инструкциям на экране:
- Введите email адрес
- Согласитесь с условиями (A)
- Выберите, перенаправлять ли HTTP на HTTPS (рекомендуется: 2)

### 7.3 Проверка автоматического обновления

```bash
sudo certbot renew --dry-run
```

Если все правильно, сертификат будет автоматически обновляться.

### 7.4 Проверка HTTPS

Откройте в браузере: `https://liquidityscan.io`

Должен быть зеленый замочек и сайт должен работать по HTTPS.

---

## 9. Troubleshooting

### Проблема: Домен не открывается

**Решение:**

1. **Проверьте DNS:**
   ```powershell
   nslookup liquidityscan.io
   ```
   Должен вернуться IP адрес сервера.

2. **Проверьте веб-сервер:**
   ```bash
   sudo systemctl status nginx
   ```
   Должен быть `active (running)`.

3. **Проверьте логи:**
   ```bash
   # Для Nginx:
   sudo tail -f /var/log/nginx/liquidityscan.io.error.log
   ```

4. **Проверьте права доступа:**
   ```bash
   ls -la /var/www/liquidityscan.io
   sudo chown -R www-data:www-data /var/www/liquidityscan.io
   sudo chmod -R 755 /var/www/liquidityscan.io
   ```

### Проблема: 403 Forbidden

**Решение:**

```bash
sudo chown -R www-data:www-data /var/www/liquidityscan.io
sudo chmod -R 755 /var/www/liquidityscan.io
```

### Проблема: 404 Not Found для маршрутов React Router

**Решение:**

Убедитесь, что в конфигурации веб-сервера включена поддержка SPA routing:

**Для Nginx:** должно быть `try_files $uri $uri/ /index.html;`
**Для Apache:** должны быть правила RewriteRule

Проверьте конфигурацию и перезагрузите веб-сервер.

### Проблема: Конфликт с существующими сервисами

**Решение:**

1. **Проверьте существующие домены:**
   ```bash
   # Для Nginx:
   grep -r "liquidityscan.io" /etc/nginx/sites-enabled/
   ```

2. **Убедитесь, что используете отдельную директорию:**
   ```bash
   ls -la /var/www/liquidityscan.io
   ```

3. **Откатите изменения если нужно:**
   ```bash
   # Для Nginx:
   sudo rm /etc/nginx/sites-enabled/liquidityscan.io
   sudo systemctl reload nginx
   ```

### Проблема: Статические файлы не загружаются

**Решение:**

1. Проверьте пути к файлам в `index.html`
2. Убедитесь, что директория `assets/` загружена на сервер
3. Проверьте права доступа к директории `assets/`:
   ```bash
   ls -la /var/www/liquidityscan.io/assets/
   ```

---

## ✅ Чеклист завершения

- [ ] Проверено окружение сервера
- [ ] Собрана production версия локально
- [ ] Файлы загружены на сервер через WinSCP
- [ ] Создана директория `/var/www/liquidityscan.io`
- [ ] Установлены правильные права доступа
- [ ] Создан и загружен конфигурационный файл веб-сервера
- [ ] Заменен `yourdomain.com` на `liquidityscan.io` в конфигурации
- [ ] Проверена конфигурация (`nginx -t` или `apache2ctl configtest`)
- [ ] Активирована конфигурация
- [ ] Перезагружен веб-сервер
- [ ] Настроен DNS в Namecheap
- [ ] Проверена работа сайта в браузере
- [ ] Проверены существующие сервисы (сайты и боты)
- [ ] (Опционально) Настроен SSL/HTTPS

---

## 📞 Дополнительная помощь

Если возникли проблемы:

1. Проверьте логи веб-сервера
2. Проверьте конфигурацию на синтаксические ошибки
3. Убедитесь, что DNS настроен правильно
4. Проверьте права доступа к файлам
5. Убедитесь, что существующие сервисы не затронуты

**Важно:** Всегда проверяйте конфигурацию перед применением и используйте отдельные директории и виртуальные хосты для нового домена!
