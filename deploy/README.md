# Deployment Files

Эта директория содержит все необходимые файлы для развертывания домашней страницы на сервере Contabo.

## Файлы

### Конфигурационные файлы веб-сервера

- **`nginx-homepage.conf`** - Шаблон конфигурации для Nginx
  - Поддержка SPA routing (React Router)
  - Безопасные заголовки
  - Готово для HTTPS (Let's Encrypt)
  - Кэширование статических файлов
  - Gzip сжатие

- **`apache-homepage.conf`** - Шаблон конфигурации для Apache
  - VirtualHost для нового домена
  - Поддержка SPA routing через mod_rewrite
  - Безопасные заголовки
  - Готово для HTTPS (Let's Encrypt)
  - Кэширование и сжатие

### Документация

- **`COMPLETE_DEPLOYMENT_GUIDE.md`** - ⭐ **ГЛАВНЫЙ ФАЙЛ!** Полное руководство от начала до конца
  - Все шаги в одном месте
  - Детальные инструкции с командами
  - Использование WinSCP для загрузки файлов
  - Проверка окружения сервера
  - Настройка Nginx и Apache
  - Настройка DNS в Namecheap
  - Настройка SSL
  - Troubleshooting
  - **Используйте этот файл для развертывания!**

- **`SAFETY_CHECKLIST.md`** - Чеклист безопасности перед развертыванием
  - Пошаговая проверка перед развертыванием
  - Гарантирует, что существующие сервисы не будут затронуты
  - Инструкции по откату изменений

- **`DEPLOYMENT_GUIDE.md`** - Альтернативное руководство (более краткое)
  - Для тех, кто предпочитает краткую версию
  - Основные шаги без деталей WinSCP

### Скрипты сборки

- **`build-and-deploy.sh`** - Bash скрипт для Linux/macOS
  - Автоматическая сборка проекта
  - Создание архива (.tar.gz)
  - Опциональная загрузка на сервер через SCP
  - Установка правильных прав доступа

- **`build-and-deploy.ps1`** - PowerShell скрипт для Windows
  - Автоматическая сборка проекта
  - Создание архива (.zip)
  - Опциональная загрузка на сервер через SCP (требует OpenSSH)
  - Установка правильных прав доступа

### Скрипты проверки окружения

- **`check-server-env.sh`** - Bash скрипт для проверки окружения сервера
  - Проверка установленных веб-серверов (Nginx/Apache)
  - Проверка существующих доменов и конфигураций
  - Проверка используемых портов
  - Проверка существующих директорий
  - Рекомендации по безопасному развертыванию

- **`check-server-env.ps1`** - PowerShell скрипт для проверки окружения
  - Выполняет проверку через SSH
  - Показывает информацию об окружении сервера
  - Помогает избежать конфликтов с существующими сервисами

## 🚀 Быстрый старт

### ⭐ Начните с полного руководства!

**Откройте файл `COMPLETE_DEPLOYMENT_GUIDE.md` - там все шаги от начала до конца!**

Этот файл содержит:
- ✅ Проверку окружения сервера
- ✅ Локальную подготовку и сборку
- ✅ Загрузку файлов через WinSCP (детально!)
- ✅ Настройку веб-сервера (Nginx/Apache)
- ✅ Настройку DNS в Namecheap
- ✅ Проверку работы
- ✅ Настройку SSL
- ✅ Troubleshooting

### Альтернативные методы

**Если хотите использовать скрипты автоматизации:**

**Windows:**

```powershell
.\deploy\build-and-deploy.ps1 -Domain "yourdomain.com" -ServerUser "root" -ServerIP "123.45.67.89"
```

**Linux/macOS:**

```bash
chmod +x deploy/build-and-deploy.sh
./deploy/build-and-deploy.sh yourdomain.com root 123.45.67.89
```

### Ручная сборка

```bash
cd liquidityscan-web/frontend
npm install
npm run build
# Файлы будут в liquidityscan-web/frontend/dist/
```

## Использование конфигурационных файлов

1. Скопируйте соответствующий файл конфигурации на сервер
2. Замените `yourdomain.com` на ваш фактический домен
3. Разместите файл в нужной директории:
   - Nginx: `/etc/nginx/sites-available/yourdomain.com`
   - Apache: `/etc/apache2/sites-available/yourdomain.com.conf`
4. Активируйте конфигурацию и перезагрузите веб-сервер

**Подробные инструкции см. в `COMPLETE_DEPLOYMENT_GUIDE.md` - там все детально по шагам!**

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Безопасность существующих сервисов

**Перед развертыванием ОБЯЗАТЕЛЬНО:**

1. ✅ **Проверьте окружение сервера** с помощью `check-server-env.sh` или `check-server-env.ps1`
2. ✅ **Следуйте чеклисту безопасности** из `SAFETY_CHECKLIST.md`
3. ✅ **Убедитесь, что новый домен не конфликтует** с существующими доменами
4. ✅ **Используйте отдельную директорию** `/var/www/yourdomain.com`
5. ✅ **Проверьте конфигурацию** перед применением (`nginx -t` или `apache2ctl configtest`)

## Важные замечания

- Все конфигурации используют отдельный виртуальный хост для изоляции от существующих сервисов
- Замените `yourdomain.com` на ваш фактический домен во всех файлах
- После настройки DNS рекомендуется настроить SSL через Let's Encrypt
- **НЕ изменяйте существующие конфигурации** - используйте только новые файлы
- **НЕ используйте существующие директории** - создайте новую директорию для нового домена
